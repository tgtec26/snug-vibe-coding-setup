# macOS / Linux용 셸 스크립트는 LF 줄바꿈을 유지해야 한다.
# Windows에서 git의 core.autocrlf=true 설정에 의해 CRLF로 변환되면
# Mac/Linux의 bash가 첫 줄 shebang(#!/usr/bin/env bash)을 인식하지 못하거나,
# 'r: command not found' 류 오류를 일으킨다.
*.sh text eol=lf

# Windows 전용 파일은 CRLF를 유지해도 무방하다.
*.bat text eol=crlf
*.ps1 text eol=crlf
